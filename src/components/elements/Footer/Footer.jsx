import styles from "./Footer.module.scss"

const Footer = () => {
    return (
        <div className={styles.footer}>
            <span>&copy; MovieMax</span>
        </div>
    );
}

export default Footer;